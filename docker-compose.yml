version: "3.5"

services:
  minetest-server:
    build:
      context: .
    volumes:
      - ./graphviz:/root/.minetest/mods/graphviz
    
    networks:
      - minetest
    image: minetest:bitcoin_transaction
    ports:
      - "30000:30000/udp"
      - "30000:30000"


  minetest-client:
    build:
      context: .
      dockerfile: Dockerfile.minetest-client
    
    image: minetest-client:bitcoin_transaction
    networks: 
      - minetest

networks:
  minetest:
    name: minetest
    driver: bridge
